@extends('layouts.app')
@section('title', '公認オイルパターン管理')
@section('content')
<div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3"><div><h1 class="h3 fw-bold mb-1">公認オイルパターン管理</h1><div class="text-muted">現行大会と大会アーカイブに保存されたオイルパターンを横断確認します。修正は各大会・アーカイブ編集から行います。</div></div><a href="{{ route('public.oil_patterns.index') }}" target="_blank" class="btn btn-outline-primary">一般公開を確認</a></div>
<form method="GET" class="card card-body mb-3"><div class="row g-2 align-items-end"><div class="col-md-2"><label class="form-label fw-bold">年度</label><select name="year" class="form-select"><option value="">すべて</option>@foreach($years as $year)<option value="{{ $year }}" @selected((string)($filters['year'] ?? '')===(string)$year)>{{ $year }}年</option>@endforeach</select></div><div class="col-md-2"><label class="form-label fw-bold">大会区分</label><select name="classification" class="form-select"><option value="">すべて</option><option value="official_tournament" @selected(($filters['classification'] ?? '')==='official_tournament')>公認トーナメント</option><option value="approved_event" @selected(($filters['classification'] ?? '')==='approved_event')>承認イベント</option></select></div><div class="col-md-3"><label class="form-label fw-bold">会場</label><select name="venue" class="form-select"><option value="">すべて</option>@foreach($venues as $venue)<option value="{{ $venue }}" @selected(($filters['venue'] ?? '')===$venue)>{{ $venue }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label fw-bold">大会名・資料名</label><input name="keyword" value="{{ $filters['keyword'] ?? '' }}" class="form-control"></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-primary">検索</button><a class="btn btn-outline-secondary" href="{{ route('admin.oil_patterns.index') }}">解除</a></div></div></form>
<div class="card"><div class="table-responsive"><table class="table table-sm table-bordered align-middle mb-0"><thead class="table-dark"><tr><th>年度</th><th>区分</th><th>大会・会場</th><th>オイルパターン</th><th>公開</th><th>操作</th></tr></thead><tbody>@forelse($patterns as $pattern)<tr><td>{{ $pattern['year'] }}</td><td>{{ $pattern['classification_label'] }}</td><td><strong>{{ $pattern['tournament_title'] }}</strong><div class="small text-muted">{{ $pattern['venue_name'] ?: '会場記録なし' }}</div></td><td><a href="{{ asset($pattern['public_path']) }}" target="_blank" rel="noopener">{{ $pattern['pattern_title'] }}</a></td><td>{{ $pattern['is_public'] ? '公開' : '非公開' }}</td><td>@if($pattern['source_type']==='archive')<a class="btn btn-sm btn-primary" href="{{ route('admin.tournament_archives.edit', $pattern['source_id']) }}">アーカイブ編集</a>@else<a class="btn btn-sm btn-primary" href="{{ route('tournaments.edit', $pattern['source_id']) }}">大会編集</a>@endif</td></tr>@empty<tr><td colspan="6" class="text-center text-muted">該当する資料はありません。</td></tr>@endforelse</tbody></table></div></div>
<div class="mt-3">{{ $patterns->links() }}</div>
@endsection
