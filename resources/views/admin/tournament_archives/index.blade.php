@extends('layouts.app')
@section('title', '公認大会・承認イベント管理')
@section('content')
<div class="d-flex justify-content-between align-items-start flex-wrap gap-2 mb-3"><div><h1 class="h3 fw-bold mb-1">公認大会・承認イベント管理</h1><div class="text-muted">保存済みの公認トーナメントと承認イベントを確認・修正します。</div></div><a href="{{ route('public.tournament_archives.index') }}" target="_blank" class="btn btn-outline-primary">一般公開を確認</a></div>
<form method="GET" class="card card-body mb-3"><div class="row g-2 align-items-end"><div class="col-md-2"><label class="form-label fw-bold">年度</label><select name="year" class="form-select"><option value="">すべて</option>@foreach($years as $year)<option value="{{ $year }}" @selected($selectedYear===(int)$year)>{{ $year }}年</option>@endforeach</select></div><div class="col-md-3"><label class="form-label fw-bold">大会区分</label><select name="classification" class="form-select"><option value="">すべて</option><option value="official_tournament" @selected($selectedClassification==='official_tournament')>公認トーナメント</option><option value="approved_event" @selected($selectedClassification==='approved_event')>承認イベント</option></select></div><div class="col-md-5"><label class="form-label fw-bold">大会名・会場・主催者・承認番号</label><input name="keyword" value="{{ $keyword }}" class="form-control"></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-primary">検索</button><a class="btn btn-outline-secondary" href="{{ route('admin.tournament_archives.index') }}">解除</a></div></div></form>
<div class="card"><div class="table-responsive"><table class="table table-sm table-bordered align-middle mb-0"><thead class="table-dark"><tr><th>年度</th><th>区分</th><th>開催日</th><th>大会名・会場</th><th>資料</th><th>公開</th><th>操作</th></tr></thead><tbody>@foreach($archives as $archive)<tr><td>{{ $archive->year }}</td><td>{{ $archive->classification_label }}</td><td>{{ $archive->start_on?->format('Y-m-d') ?: '-' }}</td><td><strong>{{ $archive->title }}</strong><div class="small text-muted">{{ $archive->display_venue_name ?: '会場記録なし' }}</div></td><td>{{ count($archive->assets ?: []) }}</td><td>{{ $archive->is_public ? '公開' : '非公開' }}</td><td><a href="{{ route('admin.tournament_archives.edit', $archive) }}" class="btn btn-sm btn-primary">編集</a></td></tr>@endforeach</tbody></table></div></div>
<div class="mt-3">{{ $archives->links() }}</div>
@endsection
