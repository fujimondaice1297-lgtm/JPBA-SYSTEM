# 本番公開前 最終準備監査（2026-08-27）

## 判定

ローカルで完了できる実装・データ・公開表示・復元準備は完了した。本番公開は、ドメイン、HTTPS、SMTP、常駐queue、scheduler、協力選手、公開日の確定後に可能となる。

## DB復旧と再発防止

- `php artisan optimize` により残っていたローカル設定キャッシュを引き継いで全テストを起動し、`RefreshDatabase` が誤って `jpba_main` を初期化する事象が発生した。直ちに処理を停止し、空になったDBは削除せず `jpba_main_empty_20260827_120000` へ隔離した。
- 2026-08-26の暗号化バックアップを、先に別DB `jpba_restore_20260827_114815` へ復元した。DB dump・公開ファイル・非公開ファイルのSHA-256、正鍵読込、誤鍵拒否、主要6表、公開3,157件、非公開4,807件、写真2,180件がmanifestと一致した状態だけを確認してから、復元DBを `jpba_main` へ切り替えた。
- バックアップ後に投入した大岡産業レディース、シーズントライアルサマーD、男女7月末ランキングを公式正本から決定的に再投入した。ID119・189の完全性監査、公式ランキング540名との集計、3種類の再ドライランはいずれも差分0である。
- 復旧後の主要件数は、選手2,286、大会26、ボール916、ゲームスコア25,013、最終成績2,192、ユーザー1。新しい暗号化バックアップ `backup_20260827_174244` を作成し、DB・公開・非公開archiveのハッシュ、暗号鍵、件数を再検証した。
- `tests/TestCase.php` に、`APP_ENV=testing` かつ解決済みDB名が `_test` で終わる場合以外はテーブル操作前に停止する強制ガードを追加した。`composer test` は `optimize:clear`、テストDB準備、全テストの順に固定した。
- 存在しない非テストDB名 `jpba_guard_probe` を設定キャッシュへ入れた拒否試験では、DB接続前に安全停止メッセージを出して終了コード2となった。試験後は設定キャッシュを消去した。
- 防止策追加後に隔離DB `jpba_test` で全テストとPDF回帰を実行した後も、上記 `jpba_main` 件数が新バックアップと完全一致することを再確認した。

## 今回完了した監査

- `jpba:release-readiness` を追加し、APP_KEY、未適用migration、主要9テーブル、旧サイト依存、storage実書込、環境、HTTPS、locale、cookie、メール、queue、cache、session、PostgreSQL、Vite資産、公認記録切替日を一括判定する。
- 通常監査は、本番未設定をWARNとして開発継続できる。`--production` は不足項目をNGとして終了コード1で安全停止する。
- `.env.production.example` と本番切替・監視・切戻し手順を追加した。秘密情報と公開日は未入力のままにした。
- GitHub ActionsへComposer脆弱性監査、npm本番依存監査、Vite本番buildを追加した。
- アプリ既定値を日本時間・日本語へ変更し、ローカル環境も `Asia/Tokyo` / `ja` へそろえた。

## 公開表示

- `public:parity-audit` の公開12ページはすべてHTTP 200、必要ラベルOK、欠落アセット0。
- 実ブラウザで `/`、`/schedule`、`/players`、`/tournament`、`/login`、`/info` を確認した。
- 公開4主要ページの画像欠落0、旧JPBAサイトリンク0、ブラウザコンソールエラー0。
- アプリ名をJPBAへ統一し、設定更新後のログイン画面で「JPBAシステム」表示を確認した。

## 公式サイト最終差分

確認日：2026-08-27

- 公式更新履歴は2026-08-26更新分まで確認した。7月28日以後は大会エントリー、シフト、講習・インストラクター等の案内更新が中心で、新たな完了大会・最終成績・ポイントランキングは確認されなかった。
- 公式大会一覧で、取込済みの2026年7月28日シーズントライアルサマーシリーズD以後に完了済み大会はない。次の公式大会は8月29日以後の日程である。
- したがって、2026年8月27日時点で大会結果、ランキング、公認パーフェクト、800シリーズ、7－10メイドに追加取込対象はない。
- TP講習会第25回受講済み一覧は8月13日版を取込済みである。

確認元：

- https://www.jpba.or.jp/update_logs.html
- https://www.jpba.or.jp/information/tournament/tournament.html

## 残る本番作業

- 本番サーバー・ドメイン・HTTPS・SMTP・queue・scheduler設定
- 本番公開日の公認記録切替日設定
- 協力選手を使った本人ログイン・ボール登録・メールの少人数試験
- 実TP講習会開催回と受講者の確定投入・少人数通知
- 公開当日の公式サイト最終差分監査、バックアップ、権限別スモークテスト
- 切替担当、監視担当、問い合わせ窓口の確定

詳細手順は `docs/operations/production_cutover_runbook_20260827.md` を正本とする。

## 検証結果

- 全186テスト・13,148 assertions、失敗0。
- `npm audit --omit=dev` は脆弱性0件、Vite本番build成功。
- `php artisan optimize`、Blade、391ルート、scheduler 7処理を確認。
- 隔離PostgreSQL `jpba_test` で標準45,860 bytes、シュートアウト398,643 bytes、シングルエリミネーション112,194 bytesのfixture PDF生成に成功。
- 通常readinessはOK 13、WARN 7、NG 0。本番モードはローカル値、HTTPS、secure cookie、SMTP、開発用Vite hot、公認記録切替日をNGとして終了コード1で安全停止。
- ローカルにComposer実行ファイルがないため、Composer監査は追加したGitHub Actions上で実行する。本番配置時にも手順書どおり実行する。
