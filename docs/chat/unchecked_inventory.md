# 古い未チェック項目の棚卸し

作成日: 2026-06-25
最終更新: 2026-06-26

## 対象

- `docs/chat/progress_board.md`
- `docs/chat/automation_roadmap.md`
- `docs/chat/context_pack.md`

## 件数

- `progress_board.md`: 未チェック 42 件（2026-06-26整理後、OCR/AI取込詳細の確認情報表示を完了）
- `automation_roadmap.md`: 未チェック 0 件
- `context_pack.md`: 未チェック 0 件

未チェックはすべて `progress_board.md` の末尾 `Active Backlog` に集約した。古い履歴メモ、重複、一時確認、commit / push、次チャット確認の未チェックは削除済み。

## 判断

古い未チェックを、そのまま上から順番に実行する必要はない。2026-06-26時点では、必要項目だけを `Active Backlog` として残す。

2026-06-23 以降にCodexが直接進めた以下の作業は、現在のログ上ではチェック済み扱いでよい。

- DB正本スナップショット更新
- 大会別シード設定画面の登録状況表示
- 大会別 `歴代優勝者シード` の登録画面・リスト作成
- 大会終了処理チェックリスト追加
- 大会終了処理チェックリストへのスコア差分・ステージ不足・final同期差分・賞金/タイトル/シード未反映候補表示
- 大会終了処理チェックリストへの選手単位のスコア未入力候補表示
- CSVスコア一時取込
- 横持ちCSV取込
- OCR原本アップロード入口
- OCR/AI解析結果JSON取込
- OCR JSONの日本語キー・別名キー対応
- Excel/Googleスプレッドシート貼り付け取込
- 年度別シード / 永久シード / 大会別追加シードの `S` 表示回帰確認
- エントリー管理 / 抽選一覧での `S 1423` 形式表示補強

## 実行不要または履歴扱い

以下は、現時点で個別に実行する必要はない。古いログの時点では未チェックだったが、現在は履歴として扱う。

- 古い `commit / push` 指示
  - 例: `progress_board.md` 309, 367, 597, 904, 920, 1442-1443 付近
- 古い `git status` / `git diff` / `次チャット開始時` 確認
  - 例: 436, 753, 1000, 1076, 1323 付近
- `storage/backups/` をコミット対象外にする注意
  - 現在も未追跡のまま維持する運用でよい
- 旧い `S` 表示設計メモ
  - 後続の実装と回帰確認で完了扱い
- 旧い大会別 `歴代優勝者シード` 登録画面メモ
  - 後続の実装で完了扱い
- THE OPEN / ST Winter など個別大会フォワードテストの古い次工程
  - 当時の作業履歴として残す。再実行する場合は、現在のDB状態で別途テスト計画を立てる

## 後続候補として残すもの

以下は未チェックのまま残す価値がある。ただし、今すぐすべてに着手する対象ではなく、大きめの後続テーマとして扱う。

### 公開サイト踏襲

- INFORMATIONカテゴリ値と添付表示サイズ方針
- 現行JPBA1サイトの見た目・導線維持に関わる細部

### インストラクター管理

- `instructor_registry` 正本化
- 既存 `instructors` 互換レイヤの撤去可否
- 講習 / 資格 / 更新履歴
- alias / 旧ライセンス表記の扱い

### 大会・成績・配分

- `tournaments` 周辺の最終スキーマ
- `tournament_awards` / `tournament_points` と `prize_distributions` / `point_distributions` の役割整理
- 正式成績、ポイント、賞金、タイトル処理の最終接続

### 速報・競技方式

- `game_scores` 起点の速報再整備
- スコア順位速報
- ラウンドロビン
- ステップラダー
- トーナメント
- ダブルエリミネーション
- シュートアウト
- 途中成績公開ページ

### PDF

- 大会別PDFの方式別Blade分割後の回帰確認
- PDF共通表示ルールの固定
- 公式PDF風スコアシートの罫線・ロゴ・会場情報・複数ページ分割

### ランキング・シード運用

- 公式ランキング取込画面を補助画面として残すか、年度末確定ランキング管理画面へ整理するか
- 男子ranking snapshotの `as_of_date` 公式PDF日付確認
- 実ランキング取込、年度末確定処理、全日本選手権用の年度途中ランキング運用
- 大会エントリー導線への優先出場順位反映

### エントリー・ボール・ProTest

- チェックイン / 当日運用 / 抽選結果公開
- `registered_balls` と `used_balls` の役割整理
- ProTestの要件整理、スキーマ確定、インポート / 運用導線

## 直近のActive Backlog

今の流れで次に進めるなら、優先順位は以下。

1. OCR/AI出力を現在のJSON仕様へ変換する実アダプタを作る
2. 実OCRエンジンを接続する前提で、画像/PDF原本バッチから解析ジョブへ渡す境界を決める
3. OCR解析結果の信頼度・警告・人間の修正導線を取込詳細画面へ寄せる
4. 実データでCSV / Excel / OCR JSON取込から `game_scores` 確定反映まで通し確認する

## 2026-06-26 整理結果

- 古い未チェック140件から、不要な履歴・重複・一時確認系を削除した。
- 必要な後続候補は `docs/chat/progress_board.md` 末尾の `Active Backlog` へ集約した。
- 整理後の未チェックは43件。その後、OCR/AI変換結果の警告・信頼度・変換元行の表示改善を完了し、現在の未チェックは42件。
- 現行JPBAサイト確認により、公開側の踏襲候補として以下を追加した。
  - トップの上部メニュー、更新履歴、プロボウラー専用ページ、主要バナー、INFORMATION、協賛バナー、外部リンク、SNSリンク
  - INFORMATIONカテゴリの `TV情報`
  - `JPBAについて` の協会概要・会長挨拶・組織図・役員名簿・定款・事業計画/予算/報告/財務PDF
  - `選手データ` の氏名・ライセンスNo範囲・性別・地区・退会者導線
  - `インストラクター` の講習情報・スクール情報・テキスト販売・ライセンス別一覧
  - `プロテスト` の受験の流れ・実施概要・申請/結果PDF
  - `トピックス` の記事本文・画像・達成記録・大会ページリンク
  - お問い合わせ、取材のお申込み、特定商取引法、プライバシーポリシー

## 参照した現行JPBAサイト

- https://www.jpba1.jp/
- https://www.jpba1.jp/association/index.html
- https://www.jpba1.jp/schedule/index.html
- https://www.jpba1.jp/player/index.html
- https://www.jpba1.jp/instructor/index.html
- https://www.jpba1.jp/protest/index.html
- https://www.jpba.or.jp/topics.html

## 運用ルール

- `progress_board.md` の未チェックは、末尾の `Active Backlog` を正本として扱う。
- 2026-06-23以降の現在作業は、`context_pack.md`、`automation_roadmap.md`、この棚卸しファイルを優先して読む。
- 古い履歴メモを再開する場合は、必ず現在DB・現在コードで再確認してから、新しい作業ログとして切り出す。
- 新しい候補は、古いメモの中に追記せず、直近の日付メモか `Active Backlog` へ寄せる。
